<h3>Create New Game for <strong><%=current_user.team.name%></strong></h3>
<div class="field">
<%= form_for @game, :url => {:controller => "games", :action => "create"} do |f| %>
<%= f.label :game_time %>
<%= f.time_field :game_time %>
<%= f.label :game_date %>
<%= f.date_field :game_date %>
<%= f.label :check_created_sport %>
<%= f.collection_select :sport_id, Sport.all, :id, :name %></br>
<%= f.hidden_field :team_id, :value => current_user.team_id %> 

Choose a field <%= f.collection_check_boxes :field_ids, Field.all, :id, :name %> or </br>
<%= f.fields_for :fields  do |d|%>
  <%= d.text_field :name, placeholder: "create a field" %>
  <%= d.text_field :location, placeholder: "field's location" %>
  <% end %>


<%= f.submit %>
<% end %>
</div>